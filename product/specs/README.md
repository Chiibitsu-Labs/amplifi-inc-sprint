# product/specs/

PRDs for `site/`, from vibeOS's `templates/prd.md`. Specs live with the code they specify —
the vibeOS repo (protocol + canon) is attached alongside, not merged into this one.

Empty as of 2026-08-05: the site shipped ahead of protocol adoption (see this repo's
`CLAUDE.md`). New features from here forward get a spec here before BUILD.
